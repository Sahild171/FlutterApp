
class User {
  String name;
  int id;
  String avatar_url;
  String node_id;

  User(
      {required this.name,
        required this.id,
        required this.avatar_url,
        required this.node_id});
}